Merge sort divides the array into halves and recursively sorts each half. It then merges the two sorted halves back together in linear time. This divide and conquer approach guarantees a worst case time complexity of n log n.
